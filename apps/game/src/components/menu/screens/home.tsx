import { PrimaryButton } from '../../ui/PrimaryButton';

export function MenuHomeScreen() {
  return (
    <div className="flex flex-col items-center gap-4">
      <PrimaryButton>Let's play the game!</PrimaryButton>
    </div>
  );
}
